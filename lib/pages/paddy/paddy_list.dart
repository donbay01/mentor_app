import 'package:flutter/material.dart';

class PaddyList extends StatelessWidget {
  const PaddyList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
